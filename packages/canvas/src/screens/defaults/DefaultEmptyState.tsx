"use client";

/**
 * Default empty state slot — the current WorkflowDetailScreen has no "no workflow selected"
 * empty state, so this renders nothing. Exists as a placeholder for the slot system.
 */
export function DefaultEmptyState() {
  return null;
}
