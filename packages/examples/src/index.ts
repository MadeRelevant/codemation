// Re-exports every example workflow.
// Story C will populate this barrel as examples are added to src/examples/.
// Each example file exports a default WorkflowDefinition (built via `workflow(...).build()`).
