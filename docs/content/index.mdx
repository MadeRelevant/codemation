---
title: Codemation
description: Build automation workflows in TypeScript with a code-first developer experience and a real runtime behind it.
---

# Codemation

Codemation is a **code-first automation framework** for developers who want to build durable workflows in TypeScript instead of stitching together one-off scripts, custom queues, ad-hoc secrets handling, and a separate operator UI.

## Fastest path to running app

If you just want to get Codemation up and running, use these commands first:

```bash
npm create codemation@latest my-app
cd my-app
pnpm install
pnpm exec codemation db migrate
pnpm exec codemation user create --email you@example.com --password 'your-secure-password'
pnpm dev
```

That gives you:

- a generated Codemation app
- a migrated local database
- a first user for the UI
- a running local development environment

Read the rest of the docs when you need to go deeper.

It is designed for teams building:

- internal automations,
- AI-powered workflows,
- integration pipelines,
- agentic flows that need observability and retries,
- production setups that can grow from local development to web + worker deployments.

| Operator experience | Code-first authoring |
| --- | --- |
| ![Placeholder for workflow canvas and run detail screenshot](/placeholders/operator-ui.svg) | ![Placeholder for workflow code and CLI screenshot](/placeholders/code-first-authoring.svg) |

> The visuals above are intentionally placeholders for now. Replace them later with screenshots from your own app and workflow examples.

## What you build

As an app developer, you mainly own:

- `codemation.config.ts`
- workflow files under `src/workflows`
- optional custom nodes
- optional custom credential types
- runtime choices such as local embedded storage vs PostgreSQL, and inline execution vs BullMQ workers

## What the framework owns

Codemation provides the runtime and product surface around your workflow code:

- a workflow engine for graph execution, continuations, and retries,
- a host runtime for config loading, persistence, credentials, and APIs,
- a framework-owned Next.js UI for operators,
- a CLI for local development, building, migrations, and worker processes,
- pluggable node packages so you can mix built-in capabilities with your own.

## High-level architecture

![Minimum setup architecture](/architecture/minimum-setup.svg)

If you want the side-by-side view of the **minimum setup**, the **production worker setup**, and the **engine layer**, go straight to [Architecture overview](/docs/overview/architecture).

## Start here

If you are new to Codemation, read these pages in order:

1. [Create your first app](/docs/getting-started/create-your-first-app)
2. [Architecture overview](/docs/overview/architecture)
3. [Engine architecture](/docs/overview/engine-architecture)
4. [Define workflows](/docs/workflows/define-workflows)
5. [Built-in nodes](/docs/workflows/built-in-nodes)
6. [Use credentials](/docs/credentials/using-credentials)
7. [Use PostgreSQL for shared environments](/docs/deployment/postgresql)
8. [Use BullMQ and workers](/docs/deployment/bullmq-workers)

## How this docs site stays focused

This site intentionally indexes only the user guide under `docs/content/`.

That keeps framework-user documentation easy to scan and easy to maintain, while deeper internal or contributor-only notes can live elsewhere in the repository without leaking into the main product docs.
