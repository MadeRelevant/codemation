export const WORKFLOW_DETAIL_TREE_STYLES = `
  @keyframes codemationSpin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .codemation-execution-tree {
    min-width: 0;
  }

  .codemation-execution-tree [data-slot="tree-item"] {
    width: 100%;
  }
`;
