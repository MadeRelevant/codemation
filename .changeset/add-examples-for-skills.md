---
"@codemation/examples": minor
---

Add 3 runnable examples for skill-reduction pointer targets: define-credential-api-key (defineCredential + test() + HttpRequest), define-batch-node (defineBatchNode rank-within-batch transform), and subworkflow-binary-passthrough (SubWorkflow with ctx.binary binary pass-through contract). All 3 typecheck clean and extract into metadata.json (38 examples total).
